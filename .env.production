# Production Environment Variables (Template)
NODE_ENV=production

# Production API URL (will be set by AWS)
API_BASE_URL=https://api.fartooyoung.org
REACT_APP_API_BASE_URL=https://api.fartooyoung.org

# Production DynamoDB (uses AWS DynamoDB, no endpoint needed)
# DYNAMODB_ENDPOINT is undefined in production

# Production JWT Secret (will be stored in AWS Secrets Manager)
# JWT_SECRET will be injected by AWS

# AWS Region
AWS_REGION=us-east-1
